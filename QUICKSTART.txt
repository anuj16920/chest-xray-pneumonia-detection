"""
╔══════════════════════════════════════════════════════════════════╗
║   CHEST X-RAY PNEUMONIA DETECTION - QUICK START GUIDE           ║
╚══════════════════════════════════════════════════════════════════╝

🚀 GETTING STARTED IN 5 MINUTES!

STEP 1: DOWNLOAD DATASET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://www.kaggle.com/datasets/paultimothymooney/chest-xray-pneumonia
2. Click "Download" (you'll need a Kaggle account)
3. Extract the ZIP file
4. You should see a folder named "chest_xray" with train/test/val folders

STEP 2: SETUP PROJECT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Copy the "chest_xray" folder to the "data/" directory in this project
   
   Your structure should be:
   chest-xray-pneumonia-detection/
   └── data/
       └── chest_xray/
           ├── train/
           ├── test/
           └── val/

2. Install dependencies:
   
   pip install -r requirements.txt

STEP 3: TRAIN THE MODEL (AUTOMATED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Just run ONE command:

   python src/04_train_model.py

This will:
✓ Load and preprocess all data
✓ Build the model with Transfer Learning
✓ Train for 25 epochs (~30-60 minutes)
✓ Save the best model to models/best_model.h5
✓ Generate training graphs

⏱️  Training Time: 30-60 minutes (faster with GPU)

📊 While Training:
- You'll see progress bars for each epoch
- Training/validation accuracy and loss will be displayed
- Best model is automatically saved when validation accuracy improves

STEP 4: EVALUATE THE MODEL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
After training, run:

   python src/05_evaluate_model.py

This generates:
✓ Confusion matrix
✓ ROC curve
✓ Precision-Recall curve
✓ Classification report
✓ Analysis of misclassified images

All results saved to results/ folder!

STEP 5: MAKE PREDICTIONS!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Option A - Interactive Mode:
   
   python src/06_predict.py
   
   Then select mode 1 and enter image paths one by one

Option B - Web Interface (EASIEST):
   
   streamlit run app.py
   
   Then open browser to http://localhost:8501
   Upload X-ray images and get instant results!

Option C - Batch Mode:
   
   python src/06_predict.py
   
   Select mode 2 and provide folder path with multiple X-rays


📁 WHERE ARE MY FILES?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
models/
├── best_model.h5          ← Your trained model (USE THIS!)
└── final_model.h5         ← Final model after all epochs

results/
├── training_history.png   ← Training graphs
├── confusion_matrix.png   ← Confusion matrix
├── roc_curve.png         ← ROC curve
├── classification_report.txt
└── ...

sample_predictions/        ← Your prediction results


💡 TIPS & TRICKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Don't have a GPU? Use Google Colab (free GPU)
   - Upload this project to Google Drive
   - Open in Colab
   - Run: !pip install -r requirements.txt
   - Run training script

2. Want faster training?
   - Use GPU (NVIDIA with CUDA)
   - Reduce epochs to 15-20 in src/04_train_model.py
   - Increase batch size to 64 if you have enough memory

3. Model taking too long?
   - Reduce EPOCHS to 15 in src/04_train_model.py (line 287)
   - The model will still work well!

4. Out of memory error?
   - Reduce BATCH_SIZE to 16 or 8 in src/04_train_model.py (line 286)


🐛 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Problem: "Dataset not found"
Solution: Make sure chest_xray folder is in data/ directory
         Update DATA_DIR path in the script if needed

Problem: "Module not found"
Solution: Run: pip install -r requirements.txt

Problem: Training is very slow
Solution: Either use GPU, reduce epochs, or use Google Colab

Problem: Can't install TensorFlow
Solution: Make sure you have Python 3.8+
         Try: pip install tensorflow==2.15.0


📚 WHAT EACH SCRIPT DOES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
01_data_exploration.py      → Explore and visualize the dataset
02_data_preprocessing.py    → Setup data augmentation
03_model_architecture.py    → Define model structure
04_train_model.py          → MAIN SCRIPT - Train everything! ⭐
05_evaluate_model.py       → Evaluate trained model
06_predict.py              → Make predictions on new X-rays
app.py                     → Web interface (Streamlit)


🎯 EXPECTED RESULTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
After training, you should see:
✓ Training Accuracy: 95-98%
✓ Validation Accuracy: 90-95%
✓ Test Accuracy: 90-95%
✓ Precision: ~90-93%
✓ Recall: ~94-97%
✓ AUC: ~95-98%

If accuracy is below 85%, try:
- Training for more epochs
- Adjusting learning rate
- Using more data augmentation


⚠️  IMPORTANT REMINDERS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. This is for EDUCATIONAL purposes only
2. NOT for actual medical diagnosis
3. Always consult healthcare professionals
4. Model predictions can have errors


🚀 READY TO START?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Download dataset from Kaggle
2. Put it in data/chest_xray/
3. Run: pip install -r requirements.txt
4. Run: python src/04_train_model.py
5. Wait for training to complete
6. Run: streamlit run app.py
7. Upload X-rays and get predictions!

That's it! You're done! 🎉


📧 NEED HELP?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Check the README.md for detailed documentation
- Review the code comments in each script
- Google the error message
- Ask on Stack Overflow or relevant forums


Good luck with your project! 🩺🤖
"""
